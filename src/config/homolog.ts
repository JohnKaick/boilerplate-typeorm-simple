import { IKey } from '../interfaces';

const key: IKey = {
  SVC_NAME: 'sis-brasil',
  PORT: 3000,
  DB_CLIENT: 'postgres',
  DB_HOST: 'localhost',
  DB_USER: 'test',
  DB_PASSWORD: 'test',
  DB_DATABASE: 'db_test',
};

export default key;
